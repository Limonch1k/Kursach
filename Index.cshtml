
@{
    ViewData["Title"] = "Index";
}

<body>
    <h1>Обработка данных...</h1>
    <img id="Kursach" src="~/Bitmap.bmp?nocache=<?php echo time(); ?>" />
    <script>var interval = setInterval("location.reload(true)", 2000);</script>
</body>
@Html.ActionLink("Next Week Songs", "Index", new { ViewBag.latestDate })
